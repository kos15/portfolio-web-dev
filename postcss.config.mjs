const config = {
  theme: {
      extends: {
        colors: {
          lightHover: '#fcf4ff',
          darkHover: '#2a004a',
          darkTheme: '#11001F',
        }
      }
    },
    darkMode: 'selector',
  plugins: { "@tailwindcss/postcss": {} },
  
};

export default config;
